/* MovieBlog homepage live-content layer: one resilient source for trends, brief, buzz and viral. */
(()=>{const esc=s=>String(s||'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));const feed=document.getElementById('trendFeed'),brief=document.getElementById('editorialFeed'),buzz=document.getElementById('buzzFeed'),viral=document.getElementById('viralFeed');if(!feed)return;const story=(x,i,label)=>`<article class="story-item"><div class="story-number">${String(i+1).padStart(2,'0')}</div><div><a href="${esc(x.url)}" target="_blank" rel="noopener noreferrer"><h3>${esc(x.title)}</h3></a><p><strong>${esc(label)}</strong> · ${esc(x.description||'Latest report from the source.')}</p><small class="source-note">${esc(x.source||'Indian entertainment source')}</small></div></article>`;const trend=(x,i)=>`<a class="trend-item" href="./pages/trending?topic=${encodeURIComponent(x.title||'')}"><span class="trend-rank">${String(i+1).padStart(2,'0')}</span><span class="trend-item-body"><strong class="trend-title">${esc(x.title)}</strong><small class="trend-meta">${esc(x.source||'Entertainment source')} · Current</small></span><strong class="trend-score">Live</strong></a>`;fetch('./api/home-feed',{cache:'no-store'}).then(r=>r.ok?r.json():Promise.reject()).then(d=>{const news=Array.isArray(d.news)?d.news:[],buzzRows=Array.isArray(d.buzz)?d.buzz:[],viralRows=Array.isArray(d.viral)?d.viral:[];const all=[...news,...buzzRows,...viralRows].filter((x,i,a)=>a.findIndex(y=>y.url===x.url||y.title===x.title)===i);feed.innerHTML=(all.length?all.slice(0,6).map(trend).join(''):'<div class="loading">Live entertainment sources are temporarily unavailable. Please refresh shortly.</div>');brief.innerHTML=news.length?news.slice(0,5).map((x,i)=>story(x,i,'Cinema update')).join(''):'<p class="muted">No fresh cinema report is available from the latest source scan.</p>';buzz.innerHTML=buzzRows.length?buzzRows.slice(0,4).map((x,i)=>story(x,i,'Public buzz / rumour')).join(''):'<div class="mini-item"><span>Public buzz</span><h3>No sourced celebrity or movie rumour is available in the latest scan.</h3></div>';viral.innerHTML=viralRows.length?viralRows.slice(0,5).map((x,i)=>story(x,i,'Viral cinema')).join(''):'<div class="loading">No sourced viral cinema signal is available right now.</div>'}).catch(()=>{feed.innerHTML='<div class="loading">Live entertainment sources are temporarily unavailable. Please refresh shortly.</div>';brief.innerHTML='<p class="muted">Live cinema reports are temporarily unavailable.</p>';buzz.innerHTML='<div class="mini-item"><span>Public buzz</span><h3>Live entertainment sources are temporarily unavailable.</h3></div>';viral.innerHTML='<div class="loading">Live viral-cinema sources are temporarily unavailable.</div>'})})();
